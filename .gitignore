# Reference material from other personal projects — kept local for context, not part of this capstone's deliverables
documentation/other_projects/

# Secrets
.env
*.env
client_secret*.json
*credentials*.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# MCP server: cached/fetched research snapshots, not source
mcp-server/.cache/

# comparison: local run-log data, not source
comparison/run_logs/

# crewai: per-task interim outputs, regenerated fresh each run, not source
crewai/outputs/

# Node / n8n
node_modules/

# OS
.DS_Store
Thumbs.db

# directories
documentation/

# local project instructions, not part of the public repo
CLAUDE.md

# local Windows-side run script, kept local only
run.ps1

# local Claude Code project config (settings, commands, agents, skills)
.claude/

# kept local only, not part of the public repo
Color_codes.md
screenshots/logos/

# screenshots: unwatermarked originals and watermarking working folders, not for GitHub
screenshots/originals_backup/
screenshots/watermarked/
screenshots/Backup/
screenshots/Original Images Backup.zip

# reflection/ stays private except the two architecture diagrams, published via diagrams/
reflection/

# unwatermarked original of the published reflection.pdf, not for GitHub
reflection - Original.pdf

# submission-portal packaging output, regenerated from tracked source, not itself source
submission/
